import { StyleSheet } from "react-native";

export const mapStyles = StyleSheet.create({
  map: {
    width: "100%",
    height: "100%",
  },
  marker: {
    width: 30,
    height: 30,
    resizeMode: "contain",
    margin: 5,
    tintColor: "#fff",
  },
});
